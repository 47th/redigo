Building a toy redis clone that is capable of handling basic commands like `PING`, `SET` and `GET`. Along the way learning about
event loops, the Redis protocol and more. Doing this project using codecrafters to get a better grip at golang.
